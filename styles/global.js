import {StyleSheet} from 'react-native';

export const globalStyles = StyleSheet.create({
  container: {
    padding: 24,
    fontFamily: 'MarkPro',
  },
  box: {
    backgroundColor: '#ccc',
    padding: 10,
    borderRadius: 5,
    marginBottom: 10,
  },

  titleText: {
    fontSize: 20,
    fontWeight: 'bold',
  },
});
